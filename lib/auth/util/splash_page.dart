import 'package:flutter/material.dart';

/// Here just to demonstrate how routing works
class SplashPage extends StatelessWidget {
  const SplashPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
